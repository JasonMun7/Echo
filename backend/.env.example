# Optional: path to service account JSON (needed for GCS signed URLs in local dev; leave unset on Cloud Run)
# ECHO_GOOGLE_APPLICATION_CREDENTIALS=./service-account.json

# GCP project ID where Echo is deployed
ECHO_GCP_PROJECT_ID=your-gcp-project-id
# Region for Cloud Run services and jobs
ECHO_CLOUD_RUN_REGION=us-central1
# Name of the Cloud Run Job that runs the agent (production only).
# Leave unset for local dev — the backend will run the agent in-process instead.
# RUN_JOB_NAME=echo-agent

# GCS bucket used by Echo for workflow assets and live screenshots
ECHO_GCS_BUCKET=your-bucket-name

# Set to true if your GCS bucket has public read access (allUsers:objectViewer).
# This avoids needing a service account key for screenshot URL generation.
# To make bucket public: gsutil iam ch allUsers:objectViewer gs://YOUR_BUCKET_NAME
# GCS_PUBLIC_BUCKET=true

# Firebase project ID (optional, for reference)
FIREBASE_PROJECT_ID=your-firebase-project-id

# Gemini API key (required for workflow synthesis)
# Get from: https://aistudio.google.com/apikey
GEMINI_API_KEY=your-gemini-api-key
