# Copy to .env.local and fill in. Next.js loads env from this directory when you run `pnpm dev` in apps/web.

# Optional: integration logos on /dashboard/integrations (https://docs.brandfetch.com/logo-api/overview)
NEXT_PUBLIC_BRANDFETCH_CLIENT_ID=
