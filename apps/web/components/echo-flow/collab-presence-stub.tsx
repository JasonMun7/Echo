"use client";

/**
 * Phase B (§7d): wire Yjs / PartyKit / Liveblocks for cursor positions + CRDT on `flow_graph`.
 * UI shell for §7c ships with avatars in the Echo Flow top bar; animated pointers activate once presence exists.
 */
export function EchoFlowCollabStub() {
  return null;
}
