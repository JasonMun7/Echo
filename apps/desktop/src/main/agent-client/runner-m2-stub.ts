/**
 * M2 — branch / loop / Vertex / GCP control-flow and publish artifacts.
 * Gated behind feature flags; remote-workflow-runner M1 uses DAG order from `flow_graph` only.
 */
export const FLOW_CONTROL_M2_ENABLED = false;
